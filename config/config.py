# global variables
config = {
    'WINDOW_SIZE': (), # set in main when program is first executed
    'TIME_PERIODS': { '1 month': '1mo',
                      '1 year': '1y',
                      'Max': 'max' } # available time periods for charts of assets in pairs name: yfinance syntax
}
